import React from "react";

const HomePage = () => {
  return (
    <main className="grow pt-18">
      <section className="relative h-screen flex items-center overflow-hidden mt-3">
        <div className="absolute inset-0 w-full h-full bg-cover bg-center bg-[url('./assets/bg.jpg')]"></div>
        {/* You can continue coding from here. */}
      </section>
    </main>
  );
};

export default HomePage;
